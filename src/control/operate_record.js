//用户操作记录模块
const model = require('../models');
const methods = require('../common/methods');
const errorText = require('../common/error');

const addRecord = () => {
    try {

    } catch (err) {
        
    }
}

module.exports = {
    addRecord
}